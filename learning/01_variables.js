console.log("Variables in JS");
/**
 * datatypes ( data to be stored in a variable)
 * number (integer + float)
 * string (char)
 * boolean (two possible answers true and false)
 * arrays
 * objects
 * null
 * undefined
 * 
 * three types of identifieers
 * const (immutable)
 * let (mutable)
 */

const nombre = "Shahzad" // datatype => string
let age = 34// datatype => number
let num1 = 34
let num2 = 10
let sum = 34;
let isMarried = false; // datatype => boolean (only true of false)
let friendsNames = ["Muhtshim", "Zaid","Abdullah"] // array of strings
let luckeyNumber = [5,63,21] // array of numbers
let myDetails = {
    name : "Shahzad Farooq" ,
    age : 22,
    favoriteColor : "violet",
    designation : "CTO",
} // js object

let noValue = null; // null (if value is meant to be empty)
let termp  = undefined; // undefined (cancelling the value)
let result = num1 + 10;
console.log(result);
let name = "something";

console.log(friendsNames[0]);







